# Future FLUX cloud execution

No active Clore execution route exists in this pivot. A future FLUX executor must consume the persisted image task contract, and may create a paid order only from an explicit user action.
