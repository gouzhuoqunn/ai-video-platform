# LTX 2.3 Stage 3B RTX 5090 付费验证计划（未执行）

状态：`paid_execution_authorized=false`。本文件不是执行授权，不能创建订单、SSH、下载或推理。

1. 重新做只读候选/钱包检查，确认没有活动订单、部署 hold 和所有成本上限。
2. 只在 Sulphur 蒸馏清单补齐 revision、大小、SHA-256、许可证、官方管线兼容性、Gemma/上采样器闭包并变为 `executable` 后继续；否则停止。
3. 构建并记录生产镜像 digest，离线校验不含权重、提示词、媒体、令牌和密钥。
4. 经单独授权后创建一台 RTX 5090 会话；只发送受限 worker/只读缓存凭据；不暴露 Jupyter、ComfyUI、Gradio 或公网推理端口。
5. 检查 CUDA、Torch、磁盘（至少 200GB）、显存、FFmpeg 与清单 SHA-256；任一失败立即停止并取消前不推理。
6. 只提交一个合成的中等清晰度、原生音频 T2V 任务；验证 MP4 音视频流、时长、尺寸、种子和结果哈希，然后停下等待人工检查。
7. 仅在人工检查允许后，测试 I2V 及高质量档；每一步保留结构化进度、心跳、取消和卸载证据。
8. 结果已确认上传且无处理中任务后，安全卸载、清理临时 jobs、取消同一订单。不得删除远端媒体；清理仍默认 dry-run。

四个可听清晰度档保持中文可见标签：低（快速预览）、中（优先首测，原生音频）、中高（细节增强）、高（优先第二批，原生音频）。参数仅在清单与真实设备验证后固化，当前不宣称已验证。
