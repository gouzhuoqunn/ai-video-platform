# Stage 4J.1 handoff

The prepared RTX5090 batch is preserved, but the single paid attempt stopped during SSH workspace preparation with `Permission denied (publickey,password)`. No media was generated. Before any future authorization, verify password/key parity for the exact returned endpoint and then use a fresh one-use authorization. Do not create a second order under the consumed authorization.
